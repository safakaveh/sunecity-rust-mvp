# cargo install sqlx-cli --no-default-features --features postgres
# sqlx migrate run
# sqlx migrate revert
[package]
name = "sunecity-rust-mvp"
version = "0.1.0"
edition = "2024"

[dependencies]
sqlx = "0.8.6"
